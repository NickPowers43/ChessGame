#version 140

//#ifdef GL_ES
precision highp float;
//#endif

uniform sampler2D Texture;

//in vec2 outTexCoord;

void main(void)
{
	//vec4 color = vec4(texture2D(Texture, gl_TexCoord[0].st), 1);

	//gl_FragColor = vec4(color, 1.0);
	gl_FragColor = vec4(1,1,1,1);
}