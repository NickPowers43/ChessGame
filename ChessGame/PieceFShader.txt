#version 140

//#ifdef GL_ES
precision highp float;
//#endif

uniform sampler2D texture;

//in vec2 outTexCoord;

void main(void)
{
	vec3 color = vec3(texture2D(theTexture, gl_TexCoord[0].st));

	gl_FragColor = vec4(color, 1.0);
}