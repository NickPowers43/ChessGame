#version 140

in vec2 inPosition;
in vec2 inTexCoord;

uniform vec2 Offset;
uniform float Scale;

//out outTexCoord;
 
void main(void)
{
	vec2 temp = inPosition * Scale;

    //vec4 temp = vec4(inPosition, 0, 1);

	gl_Position = vec4(temp + Offset, 0, 0);
	gl_TexCoord[0] = vec4(inTexCoord, 0, 0);
}